import { Box, Typography } from '@mui/material';

const HomeScreen = () => {
  return (
    <Box style={{ width: '100%', marginTop: 200 }}>
      <Typography variant="h3" style={{ width: '100%', textAlign: 'center' }}>
        Welcome to Task Management app!
      </Typography>
    </Box>
  );
};

export default HomeScreen;
